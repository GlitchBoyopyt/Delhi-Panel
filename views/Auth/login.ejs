<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= name %> - Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://api.fontshare.com/v2/css?f[]=general-sans@500,300,600,400,700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'General Sans', sans-serif;
    }
  </style>
</head>

<body class="bg-[#0b0d0f] text-gray-300 min-h-screen flex flex-col">
   <main class="flex-grow flex items-center justify-center p-6">
       <div class="bg-[#141618] border border-white/10 shadow-md py-10 px-8 md:py-12 md:px-10 max-w-md w-full rounded-xl">
        
        <!-- Icon -->
        <div class="flex justify-center">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-16 h-16 text-indigo-500">
                <path stroke-linecap="round" stroke-linejoin="round" d="M16.5 10.5V6.75a4.5 4.5 0 1 0-9 0v3.75m-.75 11.25h10.5a2.25 2.25 0 0 0 2.25-2.25v-6.75a2.25 2.25 0 0 0-2.25-2.25H6.75a2.25 2.25 0 0 0-2.25 2.25v6.75a2.25 2.25 0 0 0 2.25 2.25Z" />
            </svg>
        </div>

        <!-- Heading -->
        <h2 class="text-center text-3xl md:text-4xl font-bold text-white tracking-tight mt-6">Welcome Back!</h2>
        <p class="text-center text-gray-400 mt-2 text-sm md:text-base">Sign in to your account to continue</p>
           
        <!-- Login Form -->
        <form method="POST" action="/auth/login" class="mt-8 space-y-5">
            
            <!-- Username -->
            <div>
                <input type="text" 
                    id="username" 
                    name="username" 
                    class="w-full p-3 bg-white/10 border border-white/10 rounded-lg text-white placeholder-gray-400 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition" 
                    placeholder="Username" 
                    required>
            </div>

            <!-- Password -->
            <div>
                <input type="password" 
                    id="password" 
                    name="password" 
                    class="w-full p-3 bg-white/10 border border-white/10 rounded-lg text-white placeholder-gray-400 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition" 
                    placeholder="Password" 
                    required>
            </div>

            <!-- Submit Button -->
            <button type="submit" 
                class="w-full flex items-center justify-center gap-2 bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-3 px-5 rounded-lg transition duration-300">                     
                <span>Sign in</span>
            </button>

            <!-- Error Message Placeholder -->
            <% if (req.query.err) { %>
                <p class="text-center text-red-500 mt-2 text-sm"><%= req.query.err %></p>
            <% } %>

        </form>

        <!-- Forgot Password / Register -->
        <div class="mt-6 text-center text-sm text-gray-400">
            <a href="/forgot-password" class="hover:text-indigo-400 transition">Forgot Password?</a> 
            | 
            <a href="/register" class="hover:text-indigo-400 transition">Sign up</a>
        </div>

       </div>
   </main>
</body>
</html>
